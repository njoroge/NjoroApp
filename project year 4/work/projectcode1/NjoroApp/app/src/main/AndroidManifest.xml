<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.njoroapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ForgotPassword"></activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".OtherActivity" />
        <activity android:name=".Courses.CompnetworksActivity" />
        <activity android:name=".Courses.AnimActivity" />
        <activity android:name=".Courses.BlocActivity" />
        <activity android:name=".Courses.IoTActivity" />
        <activity android:name=".Courses.MobileComputing" />
        <activity android:name=".Courses.CloudComputing" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ItActivity" />
        <activity android:name=".ViewActivity" />
        <activity android:name=".Sign">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>